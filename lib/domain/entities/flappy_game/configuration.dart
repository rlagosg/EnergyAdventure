class Config {
  static const groundHeight = 90.0;  // Ajustado para un juego más fácil
  static const gameSpeed = 100.0;    // Velocidad reducida
  static const pipeInterval = 3.5;   // Intervalo de grupo de pipes
  static const birdVelocity = 150;   // Velocidad del personaje ajustada
  static const gravity = -70.0;      // Gravedad ajustada
  static const cloudsHeight = 70.0;
}

